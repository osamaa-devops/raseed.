import { Receipt } from "lucide-react";
import { PageTemplate } from "../PageTemplate";

export function PaymentsPage() {
  return <PageTemplate title="مدفوعات المنصة" description="متابعة المدفوعات والفواتير على مستوى SaaS." icon={Receipt} />;
}
