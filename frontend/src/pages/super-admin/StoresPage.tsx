import { Store } from "lucide-react";
import { PageTemplate } from "../PageTemplate";

export function StoresPage() {
  return <PageTemplate title="المحلات" description="إدارة المستأجرين في منصة رصيد لاحقًا." icon={Store} />;
}
