import { Layers } from "lucide-react";
import { PageTemplate } from "../PageTemplate";

export function InventoryPage() {
  return <PageTemplate title="المخزون" description="حركات المخزون والتنبيهات ستربط لاحقًا بقاعدة البيانات." icon={Layers} />;
}
